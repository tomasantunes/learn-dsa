<!DOCTYPE html>
<html>
  <head>
    <title>Learn DSA</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <%- include('../../partials/head') %>
  </head>
  <body>

    <div class="d-flex">
      <%- include('../../partials/sidebar-pt') %>

      <main class="flex-fill p-4">
        <div class="text-center">
          <h3>Stack</h3>
        </div>
        
        <div class="slide active">
          <div class="slide-content markdown">
            <script type="text/markdown">
            # Stack — O Que É?

            Uma **stack** é uma **estrutura de dados linear** que segue o princípio:

            **LIFO**

            **Last In, First Out**  
            (**Último a Entrar, Primeiro a Sair**)

            O último elemento adicionado à stack  
            é o **primeiro a ser removido**.

            mermaid
            graph TD;
              A["Base"] --> B;
              B --> C;
              C --> D["Topo"];
            /mermaid
            </script>
          </div>
        </div>

        <div class="slide active">
          <div class="slide-content markdown">
            <script type="text/markdown">
            # Stack — Analogia do Mundo Real

            Pensa numa **pilha de pratos** 🍽️

            - Adicionas pratos **no topo**
            - Retiras pratos **do topo**
            - Não consegues aceder aos pratos do meio

            É exactamente assim que uma stack funciona.

            mermaid
            graph TD;
              Plate1 --> Plate2;
              Plate2 --> Plate3;
              Plate3 --> Plate4;
            /mermaid
            </script>
          </div>
        </div>

        <div class="slide active">
          <div class="slide-content markdown">
            <script type="text/markdown">
            # Stack — Operações Fundamentais

            Uma stack suporta normalmente as seguintes operações:

            - **push** → adicionar um elemento
            - **pop** → remover o elemento do topo
            - **peek / top** → ver o elemento do topo
            - **isEmpty** → verificar se a stack está vazia

            ####O(1)####

            Todas as operações principais têm **tempo constante**.

            mermaid
            graph TD;
              Push --> Stack;
              Stack --> Pop;
              Stack --> Peek;
            /mermaid
            </script>
          </div>
        </div>

        <div class="slide active">
          <div class="slide-content markdown">
            <script type="text/markdown">
            # Stack — Operação Push

            **Push** adiciona um elemento ao **topo** da stack.

            Passos:
            1. Mover o ponteiro do topo
            2. Inserir o novo elemento

            mermaid
            graph TD;
              A --> B;
              B --> C;
              C --> D["Novo Topo"];
            /mermaid
            </script>
          </div>
        </div>

        <div class="slide active">
          <div class="slide-content markdown">
            <script type="text/markdown">
            # Stack — Operação Pop

            **Pop** remove o **elemento do topo**.

            Regras:
            - Apenas o topo pode ser removido
            - O tamanho da stack diminui

            mermaid
            graph TD;
              A --> B;
              B --> C["Removido"];
            /mermaid
            </script>
          </div>
        </div>

        <div class="slide active">
          <div class="slide-content markdown">
            <script type="text/markdown">
            # Stack — Operação Peek

            **Peek** devolve o elemento do topo **sem o remover**.

            Útil quando precisas de:
            - Inspeccionar a stack
            - Preservar o seu conteúdo

            mermaid
            graph TD;
              A --> B;
              B --> C["Topo"];
            /mermaid
            </script>
          </div>
        </div>

        <div class="slide active">
          <div class="slide-content markdown">
            <script type="text/markdown">
            # Stack — Overflow & Underflow

            Duas condições de erro comuns:

            - **Overflow** → fazer push numa stack cheia
            - **Underflow** → fazer pop numa stack vazia

            ####n ≥ 0####

            O tamanho da stack deve ser sempre válido.

            mermaid
            graph TD;
              Empty --> Underflow;
              Full --> Overflow;
            /mermaid
            </script>
          </div>
        </div>

        <div class="slide active">
          <div class="slide-content markdown">
            <script type="text/markdown">
            # Stack — Implementações

            As stacks podem ser implementadas usando:

            - **Arrays**
            - **Listas Ligadas**

            Stack com array:
            - Tamanho fixo ou dinâmico

            Stack com lista ligada:
            - Cresce dinamicamente

            mermaid
            graph TD;
              Stack --> Array;
              Stack --> LinkedList;
            /mermaid
            </script>
          </div>
        </div>

        <div class="slide active">
          <div class="slide-content markdown">
            <script type="text/markdown">
            # Stack — Aplicações

            As stacks são amplamente utilizadas em:

            - Chamadas de funções (call stack)
            - Sistemas de Undo / Redo
            - Avaliação de expressões
            - Análise sintáctica
            - Algoritmos de backtracking

            mermaid
            graph TD;
              Stack --> Recursion;
              Stack --> Undo;
              Stack --> Parsing;
            /mermaid
            </script>
          </div>
        </div>

        <div class="slide active">
          <div class="slide-content markdown">
            <script type="text/markdown">
            # Stack — Pontos-Chave

            - A stack segue **LIFO**
            - O acesso é limitado ao **topo**
            - Push, Pop e Peek são **O(1)**
            - Simples, mas extremamente poderosa

            ####Simples ≠ Fraca####

            As stacks são fundamentais em ciência da computação.

            mermaid
            graph TD;
              LIFO --> Stack;
              Stack --> Efficiency;
              Stack --> Simplicity;
            /mermaid
            </script>
          </div>
        </div>

        <div class="slide active">
          <div class="slide-content markdown">
            <script type="text/markdown">
            # Exercício — Implementar uma Stack

            Neste exercício, vais implementar uma **estrutura de dados Stack** de raiz.

            Lembra-te: uma stack segue a regra:

            # LIFO #

            **Last In, First Out**

            Vais criar uma stack que suporta as operações mais comuns.

            mermaid
            graph TD;
              A["Base"] --> B;
              B --> C;
              C --> D["Topo"];
            /mermaid

            ---

            ## Tarefa

            Implementa uma stack com as seguintes operações:

            - **push(value)** → adiciona um elemento à stack
            - **pop()** → remove e devolve o elemento do topo
            - **peek()** → devolve o elemento do topo sem o remover
            - **is_empty()** → verifica se a stack está vazia

            ---

            ## Passos a Seguir

            1. Escolhe uma estrutura de dados para armazenar os elementos  
              (dica: uma lista em Python funciona bem)

            2. Mantém o controlo do **topo da stack**

            3. Implementa a operação **push**  
              - Adiciona um elemento ao topo

            4. Implementa a operação **pop**  
              - Remove e devolve o elemento do topo  
              - Trata o caso em que a stack está vazia

            5. Implementa a operação **peek**  
              - Devolve o elemento do topo sem o remover

            6. Implementa **is_empty**  
              - Devolve `True` se a stack não tiver elementos

            ---

            ## Restrições

            - Todas as operações devem correr em **O(1)**
            - Não uses bibliotecas de stack já existentes em Python

            ####n ≥ 0####

            O tamanho da stack nunca deve ser negativo.

            ---

            spoiler
            ```
            class Stack:
                def __init__(self):
                    self.items = []

                def is_empty(self):
                    return len(self.items) == 0

                def push(self, value):
                    self.items.append(value)

                def pop(self):
                    if self.is_empty():
                        raise IndexError("Pop de uma stack vazia")
                    return self.items.pop()

                def peek(self):
                    if self.is_empty():
                        raise IndexError("Peek de uma stack vazia")
                    return self.items[-1]


            # Exemplo de utilização
            stack = Stack()
            stack.push(10)
            stack.push(20)
            stack.push(30)

            print(stack.peek())      # 30
            print(stack.pop())       # 30
            print(stack.pop())       # 20
            print(stack.is_empty())  # False
            ```
            /spoiler
            </script>
          </div>
        </div>

        
        <!-- CONTROLS --> 
        <div class="nav-controls"> 
          <button class="btn btn-outline-dark me-2" id="prev">← Anterior</button> 
          <button class="btn btn-outline-dark" id="next">Próximo →</button> 
        </div> 
        <div class="progress-container"> 
          <div class="progress-bar" id="progress"></div> 
        </div> 
        <div class="slide-counter"> 
          <span id="current">1</span>/<span id="total">1</span> 
        </div>
      </main>
    </div>
  </body>
  <script src="/javascripts/render-slides.js"></script>
</html>
